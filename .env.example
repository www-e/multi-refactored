
# env.local in the root file

# ==========================================
# NEXT.JS FRONTEND ENVIRONMENT
# ==========================================

# --- APPLICATION CONFIGURATION ---
NODE_ENV=development

# --- BACKEND API CONFIGURATION ---
# This is the URL of your FastAPI backend.
# The Next.js API routes will proxy requests to this URL.
BACKEND_URL=http://127.0.0.1:8000
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# --- NEXTAUTH CONFIGURATION ---
# This is the secret used by NextAuth to sign its session JWTs.
# It MUST be EXACTLY the same as the JWT_SECRET in the backend's .env file.
# I am using the value from your original backend .env file.
NEXTAUTH_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# The full URL of your Next.js application, required for NextAuth callbacks.
NEXTAUTH_URL=http://localhost:3000

# --- ELEVENLABS & OTHER PUBLIC SERVICES ---
# These keys are safe to be prefixed with NEXT_PUBLIC if the client needs them.
NEXT_PUBLIC_ELEVENLABS_SUPPORT_AGENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_ELEVENLABS_SALES_AGENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# This is the base URL of your site.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- SERVER-SIDE ONLY SECRETS ---
# These keys are NOT exposed to the client.
ELEVENLABS_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx



# .env in the backend directory
# ==========================================
# FASTAPI BACKEND ENVIRONMENT
# ==========================================

# --- SECURITY CONFIGURATION ---
# This is the secret used by FastAPI to validate the JWTs sent by NextAuth.
# It MUST be EXACTLY the same as the NEXTAUTH_SECRET in the frontend's .env.local file.
JWT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- DATABASE CONFIGURATION ---
# Using the value from your docker-compose.yml for local development.
# This connects to the PostgreSQL container.
DB_URL=sqlite:///./dev.db
# If you prefer to use a local SQLite file for simpler development without Docker,
# you can comment out the line above and uncomment the one below.
# DB_URL=sqlite:///./dev.db

# --- THIRD-PARTY SERVICES (used by backend) ---
ELEVENLABS_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
ELEVENLABS_SUPPORT_AGENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
ELEVENLABS_SALES_AGENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
ELEVENLABS_WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- OTHER CONFIGURATION ---
TENANT_ID=demo-tenant
PYTHONPATH=/app